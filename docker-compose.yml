version: "3.8"

services:
  nginx:
    image: nginx:alpine
    environment:
      - TZ=Europe/Moscow
      - NGINX_HOST=localhost
      - ROOT_DIRECTORY=/
    command: [
      "/bin/sh",
      "-c",
      "export NGINX_HOST; export ROOT_DIRECTORY; envsubst '$$NGINX_HOST,$$ROOT_DIRECTORY' < /nginx.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    ]
    volumes:
      - ./nginx/development.template:/nginx.template
    ports:
      - 80:80
